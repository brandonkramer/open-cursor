// Import section intentionally left minimal — tests are WIP
import { buildRunRequest } from "../../../src/bridge/request/builder.js";

// Ensure buildRunRequest is importable and has expected shape
// Existing tests use create() which was v2 API — tests need rewrite
void buildRunRequest;
